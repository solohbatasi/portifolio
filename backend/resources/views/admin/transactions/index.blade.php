@extends('layouts.admin') @section('title','Transactions') @section('heading','Coffee transactions') @section('content')<form class="filters"><select name="status"><option value="">All statuses</option>@foreach(['created','initiating','pending','processing','success','failed','cancelled','timeout','reversed'] as $s)<option @selected(request('status')===$s)>{{ $s }}</option>@endforeach</select><input type="date" name="date_from" value="{{ request('date_from') }}"><input type="date" name="date_to" value="{{ request('date_to') }}"><input name="reference" placeholder="Reference" value="{{ request('reference') }}"><input name="public_id" placeholder="Public ID" value="{{ request('public_id') }}"><input type="number" name="min_amount" placeholder="Min KES" value="{{ request('min_amount') }}"><input type="number" name="max_amount" placeholder="Max KES" value="{{ request('max_amount') }}"><button>Filter</button></form><section class="panel">@include('admin.partials.payments',['items'=>$payments])<div class="pagination">{{ $payments->links() }}</div></section>@endsection
